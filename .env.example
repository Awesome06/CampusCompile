# ==========================================
# CAMPUSCOMPILE ENVIRONMENT BLUEPRINT
# Copy this file to '.env' and fill in your secrets.
# ==========================================

# --- 1. PostgreSQL Database ---
POSTGRES_USER=campus_admin
POSTGRES_PASSWORD=your_secure_db_password

# --- 2. S3 / MinIO Storage ---
# These act as both the MinIO login credentials and the S3 access keys
S3_ACCESS_KEY=campus_storage_admin
S3_SECRET_KEY=your_secure_storage_password

# --- 3. Azure Active Directory (SSO) ---
AZURE_CLIENT_ID=your_azure_client_id
AZURE_CLIENT_SECRET=your_azure_client_secret
AZURE_TENANT_ID=your_azure_tenant_id

# --- 4. Stanford MOSS (Anti-Cheat) ---
# Register by emailing moss@moss.stanford.edu
MOSS_USER_ID=your_moss_id

# --- 5. React ---
# Base URL for the backend API (must include the `/api` prefix, since paths like `/auth/login` are appended)
VITE_API_URL=http://localhost:8080/api

# Salt used for hashing client IP addresses. MUST be a non-empty, secret value in production.
IP_HASH_SALT=changeme_ip_hash_salt

# --- 6. Redis Auth ---
# Timeout for Redis auth checks (e.g. "1s", "500ms")
REDIS_AUTH_TIMEOUT=1s