# ==========================================
# CAMPUSCOMPILE ENVIRONMENT BLUEPRINT
# Copy this file to '.env' and fill in your secrets.
# ==========================================

# --- 1. PostgreSQL Database ---
POSTGRES_USER=postgres_user
POSTGRES_PASSWORD=postgres_password
POSTGRES_DB=CampusCompile_db

# --- 2. S3 / MinIO Storage ---
# These act as both the MinIO login credentials and the S3 access keys
S3_ACCESS_KEY=access_key
S3_SECRET_KEY=secret_key

# --- 3. Azure Active Directory (SSO) ---
AZURE_CLIENT_ID=client_id
AZURE_CLIENT_SECRET=secret_client_id
AZURE_TENANT_ID=tenant_id

# --- 4. Stanford MOSS (Anti-Cheat) ---
MOSS_USER_ID=moss_id

# --- 5. React ---
IP_HASH_SALT=changeme_ip_hash_salt

# --- 6. Redis Auth ---
REDIS_AUTH_TIMEOUT=1s

# For local development, use HTTP as shown below. In production, these should use HTTPS.
BASE_URL=http://localhost:5173
REDIRECT_URL=http://localhost:8080/api/auth/callback
COOKIE_DOMAIN=

# Keep as 1 for Local Dev. Empty Otherwise.
OAUTH_INSECURE_LOCAL_DEV=

# Go API Framework Setting. Set to release for Production.
GIN_MODE=